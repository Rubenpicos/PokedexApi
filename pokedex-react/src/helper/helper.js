//To make uppeCase the name of the pokemon// Para poner en mayúsculas la primera letra de cada pokemon. 

export const primerMayuscula = (word) => {
  return word[0].toUpperCase() + word.substring(1);
};
